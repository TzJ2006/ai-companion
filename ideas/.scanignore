# /ccscan 跳过这些路径前缀。一行一个（D29）。
# 决定于 2026-08-24 的扫描范围确认。
#
# 2026-08-29 撤销了其中一条：codex-companion/ 和 cursor-companion/ 原本因为
# 「各自独立演进、两边不互相覆盖」被排除，而人已经批准把三套实现并成一个共同基座，
# 这两处正是要被统一的东西 —— 排除它们就等于让扫描永远看不见统一的另一半。
#
# 同时删掉了 skills/.agents/skills/huashu-nuwa/：那个第三方 skill 包早就不在仓库里，
# 排除一个不存在的路径不会报错，只会一直挂在表上，让人以为还有一块地方不用读。
