# Demo Idea Graph

This fixture represents Codex Companion's own three main outcomes. Regenerate the report from the
plugin root with:

```text
python scripts/companion.py render --root examples/demo
```

The example nodes remain `planned`, so they demonstrate lifecycle detail without claiming that a
demo fixture is implementation evidence.
