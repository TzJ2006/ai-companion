---
description: "Record ideas and trigger research. Use: /idea add <title>, /idea list, /idea research <slug>, /idea show <slug>"
---

Read the full skill specification at `skills/idea/SKILL.md`, then execute the
`/idea` workflow.

## Arguments

$ARGUMENTS

Follow the subcommand rules in that skill. Always target the current project
with `-p .`.
