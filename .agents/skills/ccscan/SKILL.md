---
name: ccscan
description: 建图入门 —— 把一个项目完整读一遍，读的进度由 hook 记账而不是自报，然后把项目变成一张人可以审的想法图。触发词：ccscan、扫描项目、onboard、建想法图。
---

# ccscan — 读完整个项目，把它变成一张想法图

先读引擎旁边的 FORMAT.md（`node .companion/companion.mjs paths` 会打印所有
规范路径；规范文件就在引擎同目录）。
遵循 D33「目标授权、提问与交接」和「主线、交付三段、拒绝分类与记录分档」；从当前对话与记录继承目标，
不重复索取已有决定；只读扫描不被图上的历史错误拦住结束（I-150）。

## 参数

一个路径把扫描范围限定到那个目录；`--refresh` 是在现有图上补，而不是推倒
重来。不带参数就是整个项目、从头建一张图。

## 第 0 步 — 先看有没有旧图要迁

```
node .companion/companion.mjs migrate --dry-run
```

发现旧格式的图（带 agent 后缀的 YAML、一节点一文件的 JSON），先看是否已有迁移授权和
明确来源；有就按干跑报告执行。多份旧图并存且尚未选择时，列出各来源与迁移影响、建议，
请人决定 `migrate` / `--pick` 的具体来源，绝不自动挑赢家。迁移完成或确认无旧图才开始扫描。

**命令被拒不等于没有旧图。** 只有它明说「没有发现旧格式的图」才算无旧图；其它
任何拒绝 —— 现图里已有想法、多份旧图、文件读不出来、旧图占着正名 —— 都需要先
处理的事：命令或读取故障先调查，涉及覆盖现图或来源选择才按 D33 请人决定；不要把错误输出当作批准请求。
安装器种下的空图旁边有旧图时，`migrate`
会直接迁过去并在报告里注明覆盖了空种子；那不是拒绝，照常继续。

## 第 1 步 — 建扫描清单

```
node .companion/companion.mjs scan
```

清单是程序生成的（git 追踪的加未忽略的新文件，跳过依赖目录和二进制）。
**划掉一行的唯一方式是真的读它** —— 读文件的事件由 hook 记账（D12），你自己说
「读过了」不算数。`scan` 随时重跑可以看剩余。

## 第 2 步 — 读到清单归零

逐个读，**不许抽样**。顺序是后面的解释前面的：先 README、CLAUDE.md、AGENTS.md
和 docs（写着意图），再配置和依赖清单（写着真实结构），再全部源码含测试（测试
比任何散文都更准确地说出第 3 问的预期结果），最后 git log（意图藏在提交信息里，
尤其是骂人的那几条）。

读不完就如实报告读了多少、剩多少 —— 剩余数字是程序算的，报告里不许出现和它
对不上的说法。被跳过的文件（vendor、生成物）也要列出来并说明为什么（D29）。
读了一半的图比没有图更糟，因为它看起来是完整的。

## 第 3 步 — 问人：什么叫「这个项目做完了」

复用用户已确定的目标和现有 endpoints；缺失或冲突时再给出少量候选、区别与建议请人选择。
不要擅自把从代码推测出的功能当作用户确认的项目终点。

## 第 4 步 — 提取想法，建图

每个想法回答前三问（是什么 / 为什么 / 预期结果），能答八问的答八问。

**抽出想法之后、连前置之前，先搭树。** 先定三到七个顶层想法（各自也是用 `new`
建的想法：名称是一个短名字，`what` 是一句「做完之后项目多了什么能力」），再让每个
想法用 `parent` 挂到某个顶层想法下面；一个顶层下面超过七个，就在中间再加一层。
名称和那句话怎么写，规矩只在 FORMAT.md「The tree」那一节，这里不重抄；只补一句
这条命令特有的话：这几大步是从你刚读完的这个项目里看出来的，不是从别的仓库抄来的
模板。`check` 会报挂错的 `parent`、成环和超过七个。

新建想法一律用引擎发号：

```
node .companion/companion.mjs new "一句人话的名称" --needs I-001
```

图上每句话都必须让没打开过这个仓库的人看懂 —— 写它做什么，不写它叫什么。

**答不出来就留空，绝不编。** 这四栏是最容易被编出来的：

- `why_this_way` 是你最想撒谎的地方。真实理由在注释、提交信息和设计文档里；
  找不到就写 `why_this_way: null`，并把这个想法列进最后的报告。编出来的理由
  会被下一个 agent 当成事实，然后一路捍卫下去。
- `code` 必须解析得到：真实文件、真实行号（D32）。ccfix 完全信这一栏。
- `verify` 只写真的存在的东西：有测试就写那条测试，没有就用 `manual` 写清人
  该看什么。**绝不编造一条不存在的验证命令。**
- `expected` 从测试和文档里读；两处都没说，就问人，或者如实标成未答。

## 第 5 步 — 渲染并交接

```
node .companion/companion.mjs check
node .companion/companion.mjs render
```

`check` 报的错要在给人看之前全部修掉。然后把网页路径给人，按顺序报告：
真实的扫描数字（读了几个、跳过几个、剩几个）、终点和各自有多少想法通向它、
按状态分的计数和当前前沿、**以及 `why_this_way` / `expected` / `verify` 没
答上来的想法逐个列出** —— 这份清单是下一轮 ccthink 的议程，也是这条工作流
最值钱的产物。

交接摘要追加进相关想法的 log，结束回复按 D33「交付三段」写。这条工作流只写想法图、网页和 log，不碰产品代码、不建测试、
不重构。用户只请求扫描，就交付结果结束；扫描属于已授权实现的一部分，就交给 ccthink / ccbuild
继续目标内工作，不额外要求“批准扫描”。读完项目不代表获得了修改整个项目的授权。

## 同目录协作（`coord enable` 开着时才生效）

协作模式下想法图不许用编辑工具裸写：新建想法用 `new`，改字段用 `edit`，成批改动走网页
提交。开工前 `coord join`，建图这件事本身先 `coord say` 告诉同伴，免得两个人同时扫同一个仓库。
